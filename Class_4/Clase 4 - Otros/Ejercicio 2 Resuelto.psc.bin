Algoritmo Ejercicio2
	//1. Definir e inicializar variables
	Definir array, i, mayor Como Entero
	Dimension array[5]
	i = 0
	mayor = 0
	
	//2. Inicializar el array con números pedidos por consola
	Para i = 0 Hasta 4 Con Paso 1 Hacer //Recordad que el valor_final de la variable "i" es (dimension - 1)
		Escribir "Introduce un número" //Pedimos los números por consola
		Leer array[i] //Guardamos el número en la posición "i" del array
	FinPara
	
	//3. Buscar cuál es el número mayor
	Para i = 0 Hasta 4 Con Paso 1 Hacer
		Si (array[i] > mayor) Entonces
			mayor = array[i]
		FinSi
	FinPara 
	
	//4. Escribir todos los números del array
	Para i = 0 Hasta 4 Con Paso 1 Hacer
		Escribir array[i], " " Sin Saltar
	FinPara
	Escribir "" //Para hacer un salto de línea
	
	Escribir "El número mayor del array es el: ", mayor
	
FinAlgoritmo
